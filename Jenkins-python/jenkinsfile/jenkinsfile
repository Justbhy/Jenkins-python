pipeline {
    agent any 
    stages {
        // stage('build') {
        //     steps {
        //         sh 'pip install flask'
        //     }
        // }
        stage('test') {
            steps {

               echo 'test is good' 
            }
        }
    }
}